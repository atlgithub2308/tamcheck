{
  "description": "Get the count of Puppet Enterprise server nodes that are not expired.",
  "parameters": {
    "output_dir": {
      "type": "String",
      "description": "Directory to store tamcheck_data output."
    }
  },
  "implementations": [
    {
      "name": "pe_server_node_count_not_expired.py",
      "requirements": ["shell", "puppet-agent"],
      "input_method": "environment"
    }
  ]
}
